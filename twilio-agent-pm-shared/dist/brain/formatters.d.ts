/** Emoji icon for a MIME type / filename. */
export declare function fileIcon(mime: string, name: string): string;
/** Emoji icon for an attachment URL based on its domain. */
export declare function attachLinkIcon(url: string): string;
/** Human-readable file size string. */
export declare function fmtBytes(bytes: number | null): string;
/** Formats seconds as H:MM:SS or M:SS. */
export declare function fmtTime(seconds: number): string;
/** Formats an ARR string (dollars) into a short currency form. */
export declare function formatArr(arr: string | null): string;
//# sourceMappingURL=formatters.d.ts.map